import placesReducer from './reducer';

export { default as placesTypes } from './types';
export { default as selectors } from './selectors';
export { default as actionCreators } from './action-creators';

export default placesReducer;
